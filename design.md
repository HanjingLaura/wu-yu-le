# WuyuLe 界面设计约束

这份文档定义手机端「物」浏览体验的产品与界面硬规则。实现、文案和视觉调整都以这里的约束为准。

## 产品地图

以下七个入口组成 MVP 的完整路径。入口名称是设计与代码约定；实际界面只保留必要的图标和短文案，并继续遵守本文的禁词规则。

1. **架子（首页）**：首页只陈列扣好的透明物品，每行固定四格且格内无字。每个物格点击后进入同一条记录的事件详情。书图标和搜索只出现在这一页的顶栏；不要圆形字母头像。
2. **书图标 → 年表**：仅架子首页顶栏的书图标打开按发现时间回看的年表。年表页本身不再显示书/搜索顶栏，只保留返回。不使用「时间线」等禁词大标题；返回后回到原来的首页位置。点对应事件进入与架子、相册相同的事件详情。
3. **事件详情**：架子、年表、相册三个入口打开同一块详情界面（同一组件/同一套布局），不要三套 reader。详情纵向居中，顶端展示 `objectImage`（有扣物时），中段展示日期、短标题和正文 `content`，下方展示一张或多张现场照片 `storyImages`，再提供喜欢与回应。文字、配图与回应区可继续向下滚动；只在顶栏左侧保留返回图标，不显示架子首页那条书/搜索顶栏。
4. **相册**：公开趣事组成两列、自适应高度的 masonry 照片帖，只使用 `storyImages`，不展示扣物 `objectImage`。帖子的图片上方、简短文字下方保持 kraft 卡片样式。点对应事件（照片或回应入口）进入同一块事件详情，完整喜欢/回应在详情里完成。相册页不显示架子首页那条顶栏。
5. **+ 新增**：新增流程依次完成：选择或拍摄物图并自动扣图得到 `objectImage`；另选或拍摄一张或多张现场照片作为 `storyImages`；填写日期、短标题、正文 `content`、参与好友及公开/私藏；提交后同时上架物品并生成关联 Story，公开 Story 进入相册。新增面板几乎无字：不放「放上一件」「新增物品」或其他装饰性大标题/分区标题；保留输入占位、日期控件、公开/私藏选项和「上架」主按钮。两个虚线上传区要能分清：物品图标「上传物品图片」（扣图后作为 `objectImage`），趣事配图标「上传图片」（可多张，作为 `storyImages`）。参与好友用「加入好友」列表行（右侧 `>`）从现有好友里多选，确认后以姓名/标签回到面板；不要逗号分隔的自由输入。面板打开时不显示架子首页那条顶栏。
6. **朋友**：提供好友列表与邀请。页面不放「朋友」等分区大标题或邀请营销文案，也不显示架子首页顶栏。加好友图标与按姓名搜索放在同一行。
7. **我的**：提供个人资料、设置与退出。页面不放「我的」等分区大标题，不显示架子首页顶栏，也不用圆形字母头像作为入口。设置与退出作为同一组列表行。

### 统一数据关系

- `ShelfObject` 表示架上可点击的物，`ShelfObject.objectImage` 始终是透明扣物 PNG；架子只渲染这个字段。
- `Story` 表示与物关联的一次趣事，至少包含 `content`、日期、短标题、参与好友、公开/私藏状态和 `storyImages: string[]`；`storyImages` 始终是现场照片，详情和相册都从这里读取。
- 每个上架物通过稳定的关联 id 对应一条 Story。新增提交必须同步更新 `shelfObjects` 与 `stories`（或等价的统一状态），不能只创建 `objectImage` 而丢失正文或现场照片。
- 公开 Story 才能出现在相册；私藏 Story 仍可由有权限的用户从物格打开详情。旧路由 `/gallery`、`/book` 等必须读取同一组 `ShelfObject`/`Story` 数据，不维护第二套字段或样例。

## 内部概念与可见文案

`收藏`、`展架`、`史书`、`物语了`、`时间线` 仅是产品内部概念或代码/数据模型用语。它们不得出现在任何用户可见的 UI 文案中，也不得出现在按钮文字、页面标题、`title` 悬停提示、`aria-label` 或空状态文案中。按时间浏览的页面统一称为「年表」或「按时间查看」；书图标的 `aria-label` 固定为「打开年表」，不设置 `title`。需要返回时只保留顶部左侧返回图标（可使用图标-only 控件，不在详情底部重复放置白底「返回」按钮）。

## 产品逻辑：观看物与年表

产品核心是观看一件件「物」：实物、照片、文字、一个瞬间或一段共同经历。界面先让物和内容被看见，再提供必要的时间、参与者和操作信息。年表负责按时间回看发生过的事；物的文字内容只在点开后的详情中阅读。年表页不显示「时间线」大标题，只保留年份、日期和记录卡片。

每件物都保留来源、时间和参与关系。公开的物可以被发现，私人的物只对拥有访问权的人开放。公开状态、日期和参与者属于详情数据，不得挤进主页物格。

## 主页与物格

- 主页以 kraft 木架和物品图片为主，页眉尽量只保留图标，不堆叠品牌或功能文字。
- 书图标 + 搜索这一条顶栏只出现在架子首页；相册、朋友、我的、年表、事件详情和新增面板都不得再渲染这条顶栏，也不使用圆形字母头像作为 chrome 入口。
- 左上角书图标只负责打开年表，`aria-label` 固定为「打开年表」，不要设置 `title` 悬停提示。年表页不显示「时间线」标题或其他禁词。
- 架子是固定四列的网格：每一行恰好 4 个格位（row size = 4）。物不足四件时保留空格，不改变列数；窄屏也保持四格，可横向滚动或缩小格位。
- 架子上的物品是从照片中扣出的透明 PNG，不是照片方块：图片使用 `object-fit: contain`，底边对齐木架，去掉相框和 `cover` 裁切，让物品像实物一样立在格内。架上不显示任何文字。
- 每个格位只显示物品图。格内不得显示日期、标题、参与人、状态角标、件数或任何辅助文字；不要把图片做成带字海报。示例数据一开始就提供至少 6–8 行（每行 4 格，共 24–32 件）`cutout: true` 的透明物品，数量足够时可继续向下无限加载更多样例/占位物；相册仍只使用原始现场照片帖。
- 点击物格进入事件详情。物格本身是唯一入口，需有足够大的点击区域和清晰的键盘焦点。

## 事件详情

- 架子点物、年表点对应事件、相册点对应事件，必须打开同一块 kraft 羊皮纸事件界面，而不是三套不同的 reader。
- 信息顺序固定为「上物、中间文字、下方趣事配图、喜欢与回应」：最上方居中展示架上那件扣好的透明物品大图（`objectImage`，若有）；中间依次显示日期、短标题和正文；文字下方显示这件事的现场照片（`storyImages`，可一张或多张）；再提供喜欢和回应，交互在三个入口里相同。`objectImage` 是透明扣物，`storyImages` 是相册照片，两者不可混用。
- 羊皮纸默认纵向居中。文字、趣事配图与回应组成可纵向滚动的阅读区域，内容较长时可以向下扒拉阅读；顶部物品展示保持在结构最上方。
- 纸张应有 aged parchment 的真实触感：暖米色底、低对比褐色斑点、轻微皱褶和不规则纤维；纹理保持克制，不能压过图片与正文。可使用仓库参考图 `/workspace/wuyule-brand/_refs/parchment/parchment-ref-2.jpg`，或以 CSS 叠加同等效果。
- 详情只在顶部提供左侧返回图标（`aria-label="返回"`），返回后仍回到原来的入口位置；详情底部不放白底「返回」按钮。
- 详情之外不重复标题、日期和参与人；主页保持纯物陈列。
- 不放「评论」等装饰性分区标题；喜欢与回应只保留控件、计数、已有回应和输入。

## 相册照片流

- 相册使用两列自适应高度的 masonry 照片帖子流；每张卡片的高度随图片比例和下方文字自然变化，不强制统一卡片高度。
- 卡片采用简约 kraft 圆角样式，内容顺序为上图下文；日期、标题、摘要以及爱心/回应计数保持短小克制。点照片或回应入口进入与架子、年表相同的事件详情，完整喜欢与回不应只停在相册卡片里。
- 相册页面不放品牌名、大标题或分组宣传文案；底部保留五个图标入口（陈列、相册、`+`、朋友、个人），只显示图标，不显示任何文字标签。每个图标使用简短、清晰且不含禁词的 `aria-label`。

## 底部导航

- 底部导航固定五个入口：陈列、相册、`+`、朋友、个人；视觉上只保留图标，中间 `+` 作为主要操作。
- 不渲染架子、相册、朋友或个人等文字标签，也不使用 hover `title` 代替标签；可访问名称只放在 `aria-label`，并遵守禁词规则。界面 chrome 不出现「语」或「物语了」；鉴权页品牌只保留英文 `WuyuLe`。
- 图标保持足够点击区域和键盘焦点；选中态仅用颜色、线条或轻微背景区别，不增加文字。
- 底栏是悬浮的浅奶油色完整胶囊/椭圆：上沿连续闭合，不要 U 形凹槽、缺口或任何从条上挖掉的路径。高度紧凑，侧边图标贴近胶囊垂直中线，不要在条内留下大块顶部空白。
- 中间 `+` 是叠在这条实心底栏之上的独立圆形按钮，部分重叠、略微高出上沿；它不是嵌进凹槽里的，也不要用 mask/clip 给底栏让位。
- 架子等内容区向下滚动时，底栏保持固定，不随物格一起移走。

## 文案与视觉

- 文案短、具体、克制，描述真实的物、时间、动作和参与关系；不使用模板化热情口吻、营销腔或空泛口号。
- 界面 chrome 接近无字：不放装饰性大标题、分区标题或营销句（例如「放上一件」「新增物品」「邀请朋友」连同口号）。可见文字只留给真实内容、表单占位、作为控件本身的选项（如公开/私藏）和必要的主操作（如上架）。鉴权页品牌只保留英文 `WuyuLe`，不出现「语」或「物语了」。
- 方向是 kraft 简约：纸张、卡纸、木架和手工陈列质感，颜色、边框和阴影保持克制，为图片和正文留出呼吸空间。
- 视觉应轻、暖、安静；不要使用产品黑底、霓虹渐变、强玻璃拟态或装饰性噪点制造“科技感”。
- 保持手机优先。图标、物格、返回控件在窄屏上都要有明确的点击区域、焦点样式和可读的无障碍名称；无障碍名称也必须遵守内部概念禁词规则。

## 物品上架与自动扣图

- 点击底栏 `+` 后，可以直接拍照或从相册选图；选图后默认在浏览器内自动去背景。
- 自动处理完成后得到透明 PNG，先展示预览，再填写发现/发明日期、短标题和参与好友；提交后把物品加入四列网格。
- 自动扣图依赖模型首次加载可能需要等待；模型或网络不可用时，明确显示状态，并提供居中裁切的可用 fallback，不能让用户误以为已经生成透明图。
- 数据模型明确区分架上物品和趣事：`ShelfObject.objectImage` 保存透明扣物 PNG，`Story.storyImages` 保存现场照片数组；Story 通过关联把两者放在同一张羊皮纸中。物品的最小本地数据还包括发现/发明日期、短标题和参与好友，MVP 可以保存在页面 state 中。日期、短标题和参与好友只在羊皮纸详情中展示。
- sample 数据一开始至少提供 6–8 行（24–32 件）`cutout: true` 的 `ShelfObject`，每件关联一条含 `storyImages` 的 Story；物足够多时架子在接近底部处继续加载更多样例/占位物，形成向下的无限滚动。架子只渲染 `objectImage`，相册只渲染 `storyImages`。

## 验收清单

1. 打开主页时看到的是立在 kraft 木架上的透明扣物网格；每一行固定 4 个物格，初始至少 6–8 行样例；物品 `contain` 且底对齐，格内无日期、标题、参与人、角标、件数或其他文字。数量足够时继续向下滚动会加载更多物格，底栏保持固定。
2. 书图标 + 搜索顶栏只出现在架子首页，没有圆形字母头像；书图标的 `aria-label` 固定为「打开年表」，没有 `title` 悬停提示。相册、朋友、我的、年表、事件详情和新增面板都不显示这条顶栏。
3. 点击书图标进入年表；页面不显示「时间线」大标题，也不再显示架子首页顶栏，页面可见标题和控件不包含全部禁词，从年表返回后仍回到主页网格。
4. 点击物格、年表卡片或相册对应事件，进入同一块 kraft 事件详情；按「上物、中间文字、下方趣事配图、喜欢与回应」显示 `objectImage`、日期/短标题/正文、一张或多张 `storyImages`，以及喜欢和回应；详情默认纵向居中，文字、配图与回应区可向下滚动；仅保留顶部左箭头返回，不再显示底部白色「返回」按钮，也不使用旧翻页 reader。
5. 相册是两列自适应高度的照片帖子 masonry，卡片为 kraft 圆角，上图下文且不出现品牌名、大标题、分组宣传文案或架子首页顶栏。
6. 底部导航只显示五个图标（中间为 `+`），不显示任何文字标签；每个图标有合适的 `aria-label`，且名称不含禁词。视觉为完整连续的奶油色椭圆底栏，中间 `+` 作为独立圆按钮叠在条上，条内无大块顶部留白，也没有凹槽。
7. 全部用户可见文案、`aria-label`、`title` 属性和空状态文案都不包含 `收藏`、`展架`、`史书`、`物语了`、`时间线`。
8. 通过 `+` 拍照或选图后，默认进入自动扣图；透明 PNG 预览完成后可以作为 `objectImage` 加入四列网格，关联的现场照片作为 `storyImages` 出现在详情文字下方和相册中。
9. 新增面板、朋友、我的等 chrome 不出现装饰性大标题或营销句；表单仍可用占位文字、日期、公开/私藏和「上架」完成提交。两个上传虚线框分别标「上传物品图片」和「上传图片」，不要只放图标。参与好友是「加入好友」行加 `>`，点开从现有好友多选，确认后显示已选姓名/标签，不再用「用逗号分开，可不填」输入框。朋友页加好友图标与搜索同一行；我的页设置与退出同组列表，不用圆形字母头像当入口。
