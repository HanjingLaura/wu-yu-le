# WuyuLe 界面设计约束

这份文档定义手机端「物」浏览体验的产品与界面硬规则。实现、文案和视觉调整都以这里的约束为准。

## 内部概念与可见文案

`收藏`、`展架`、`史书`、`物语了` 仅是产品内部概念或代码/数据模型用语。它们不得出现在任何用户可见的 UI 文案中，也不得出现在按钮文字、页面标题、`title` 悬停提示、`aria-label` 或空状态文案中。需要返回时统一使用「返回」或图标-only 控件。

## 产品逻辑：观看物与时间线

产品核心是观看一件件「物」：实物、照片、文字、一个瞬间或一段共同经历。界面先让物和内容被看见，再提供必要的时间、参与者和操作信息。时间线负责按时间回看发生过的事；物的文字内容只在点开后的详情中阅读。

每件物都保留来源、时间和参与关系。公开的物可以被发现，私人的物只对拥有访问权的人开放。公开状态、日期和参与者属于详情数据，不得挤进主页物格。

## 主页与物格

- 主页以 kraft 木架和物品图片为主，页眉尽量只保留图标，不堆叠品牌或功能文字。
- 架子是固定四列的网格：每一行恰好 4 个格位（row size = 4）。物不足四件时保留空格，不改变列数；窄屏也保持四格，可横向滚动或缩小格位。
- 每个格位只显示物品图。格内不得显示日期、标题、参与人、状态角标、件数或任何辅助文字；不要把图片做成带字海报。
- 点击物格进入羊皮纸详情页/层。物格本身是唯一入口，需有足够大的点击区域和清晰的键盘焦点。
- 左上角书图标只负责打开时间线，`aria-label` 必须是「打开时间线」，不要设置 `title` 悬停提示。时间线页面的可见标题使用「时间线」，不得使用内部概念词。

## 羊皮纸详情

- 详情采用 kraft 羊皮纸样式，替换旧的左右翻页 reader。
- 信息顺序固定为「上图下文」：先显示配图，再显示短标题和正文；正文可包含日期、参与者和完整叙述。
- 详情提供「返回」按钮或图标-only 返回控件，返回后仍回到原来的主页网格位置。
- 详情之外不重复标题、日期和参与人；主页保持纯物陈列。

## 文案与视觉

- 文案短、具体、克制，描述真实的物、时间、动作和参与关系；不使用模板化热情口吻、营销腔或空泛口号。
- 方向是 kraft 简约：纸张、卡纸、木架和手工陈列质感，颜色、边框和阴影保持克制，为图片和正文留出呼吸空间。
- 视觉应轻、暖、安静；不要使用产品黑底、霓虹渐变、强玻璃拟态或装饰性噪点制造“科技感”。
- 保持手机优先。图标、物格、返回控件在窄屏上都要有明确的点击区域、焦点样式和可读的无障碍名称；无障碍名称也必须遵守内部概念禁词规则。

## 物品上架与自动扣图

- 点击底栏 `+` 后，可以直接拍照或从相册选图；选图后默认在浏览器内自动去背景。
- 自动处理完成后得到透明 PNG，先展示预览，再填写发现/发明日期、短标题和参与好友；提交后把物品加入四列网格。
- 自动扣图依赖模型首次加载可能需要等待；模型或网络不可用时，明确显示状态，并提供居中裁切的可用 fallback，不能让用户误以为已经生成透明图。
- 物品的最小本地数据包括透明图片、发现/发明日期、短标题和参与好友；MVP 可以保存在页面 state 中。日期、短标题和参与好友只在羊皮纸详情中展示。

## 验收清单

1. 打开主页时看到的是纯物品图片网格；每一行固定 4 个物格，格内无日期、标题、参与人、角标、件数或其他文字。
2. 页眉尽量无字；左上角书图标的 `aria-label` 为「打开时间线」，没有 `title` 悬停提示。
3. 点击书图标进入「时间线」；页面可见标题和控件不包含内部概念禁词，从时间线返回后仍回到主页网格。
4. 点击物格进入 kraft 羊皮纸详情，按上图下文显示配图、短标题和正文；返回控件文字为「返回」或图标-only，且不再使用旧翻页 reader。
5. 全部用户可见文案、`aria-label`、`title` 属性和空状态文案都不包含 `收藏`、`展架`、`史书`、`物语了`。
6. 通过 `+` 拍照或选图后，默认进入自动扣图；透明 PNG 预览完成后可以加入四列网格并在详情中查看文字。
