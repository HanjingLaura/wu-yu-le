# 物语了设计约束

这份文档是 WuyuLe（物语了）的产品与界面硬规则。实现、文案和视觉调整都应以这里的约束为准。

## 产品逻辑：博物馆式的「物」

产品的核心是收集和观看「物」：一件物可以是实物、照片、文字、一个瞬间或一段共同经历。用户把发现、发明和生活中值得留下的东西放进博物馆；参与的好友可以一起补充、观看和记住这件物。产品提供的是安静的收藏与回看体验，不是信息流、效率工具或内容营销平台。

每件物都应有清楚的来源、时间和参与关系。公开的物可以被发现，私人的物只对拥有访问权的人开放。界面先让物和内容被看见，再提供必要的时间、参与者和操作信息。

## 主页与史书入口

- 主页是「架子」：把用户收藏的物按自然的陈列关系摆在架子上，重点是物本身的形状、照片、标题和时间。
- 左上角的书图标代表「史书」，是进入史书时间线的唯一顶栏入口；点击书图标仍必须打开史书时间线。
- **主页左上角禁止出现「物语了」文字。左上角只放书图标，不放文字锁、不放副标题。**
- 主页架子与史书是两个相互连接的观看视角：架子用于陈列物，史书用于按时间回看发生过的事。不要用额外的品牌文字替代这两个入口。

## 文案硬规则

- **禁止 AI 味文案。** 不使用模板化的热情口吻、空泛的价值宣言、营销腔、过度解释或像机器生成的祝福和提示。
- 文案要短、具体、克制，优先描述真实的物、时间、动作和参与关系；能用一个准确的词就不写一段话。
- 不在界面上堆叠产品口号、功能口号或重复的品牌名。品牌名可以出现在需要明确产品身份的页面和文档中，但主页左上角是书图标专属区域。
- 保留中文语境的自然感，避免为了显得“有趣”而强行玩梗。无语字方框、脸、汗滴等表情化符号不作为品牌或导航元素。

## 视觉与交互

- 方向是 kraft 简约：纸张、卡纸、木架和手工陈列的质感，克制的颜色、边框和阴影，给内容留出呼吸空间。
- 视觉应轻、暖、安静，优先保证物的可辨识度与阅读秩序；不要使用产品黑底、霓虹渐变、强玻璃拟态或装饰性噪点来制造“科技感”。
- 保持手机优先，并让架子、书图标和物卡在窄屏上仍有明确的点击区域与层级。书图标需要有可见的无障碍名称，但可视区域只显示图标。
- 新增装饰必须服务于陈列、时间或参与关系；如果一个元素不能帮助用户理解或观看物，就不应放进主页。

## 物品上架与自动扣图

- 点击底栏 `+` 后，可以直接拍照或从相册选图；选图后默认在浏览器内自动去背景。
- 自动处理完成后得到透明 PNG，先展示预览，再填写发现 / 发明日期、短标题和参与好友，提交后把物品放到架子上。
- 自动扣图依赖模型首次加载可能需要等待；模型或网络不可用时，界面要明确显示状态，并提供居中裁切的可用 fallback，不能让用户误以为已经生成透明图。
- 物品的最小本地数据包括透明图片、发现 / 发明日期、短标题和参与好友；MVP 可以保存在页面 state 中。

## 验收清单

1. 打开主页时看到的是架子与物，顶栏左上角只有书图标。
2. 点击书图标进入史书时间线；从史书返回后仍回到主页架子。
3. 主页左上角不存在「物语了」「展架」或其他文字品牌锁。
4. 新增或修改的文案具体、克制，没有 AI 味和营销腔。
5. 视觉保持 kraft 简约，且不引入无语脸/汗滴图形或产品黑底。
6. 通过 `+` 拍照或选图后，默认进入自动扣图；透明 PNG 预览完成后可以上架并出现在架子上。
